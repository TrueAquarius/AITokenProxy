{
  "name": "ai-token-proxy",
  "version": "0.2.0",
  "description": "Proxy between AI Agents and LLMs that logs per-request token consumption with attribution metadata.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsx watch src/index.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "gpt-tokenizer": "^2.6.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
